<h1><%= @club.name %></h1>
<% unless current_user.clubs.include?(@club) %>
	<%= button_to 'Join Club', join_club_path(@club.id), :method => :post%>
<% end %>

<%= link_to "View Books", club_books_path(@club) %>

<% @club.books.each do |f| %>
	<%= f.title %>
<% end %>
